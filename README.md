

```bash
echo "# study.java" >> README.md
git init
git add README.md
git commit -m "Spring-FrameWrok"
git branch -M main
git remote add origin https://github.com/linjae2/study.Spring-FrameWrok.git
git push -u origin main
```

```sh
$ gradle help --task init

$ gradle init --type java-application --no-incubating --no-split-project --java-version 17 --dsl groovy --test-framework junit-jupiter --project-name ScheduleExecutorService
```