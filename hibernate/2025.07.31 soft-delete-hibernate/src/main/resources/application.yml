server:
  port: ${PORT:8080}
  servlet:
    context-path: /api

spring:
  # datasource:
  #   driver-class-name: com.mysql.cj.jdbc.Driver
  #   url: jdbc:mysql://localhost:3306/deletion?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
  #   username: admin
  #   password: 1234
  datasource:
    hikari:
      maximum-pool-size: 10
    url: jdbc:mariadb://172.31.37.55:3401/test?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: sdman
    password: snudh123
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    generate-ddl: true
